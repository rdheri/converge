# Postgres connection string. If unset, the server falls back to an
# in-memory op store (fine for a quick demo; nothing survives restart).
DATABASE_URL=postgres://converge:converge@localhost:5432/converge

# WebSocket + health-check port.
PORT=8787
